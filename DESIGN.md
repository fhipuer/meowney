# Meowney Product Design Guide

이 문서는 Meowney의 실제 제품 UI를 구현하고 검토할 때 사용하는 기준이다. `DESIGN-tesla.md`는 시각적 절제와 타이포그래피에 대한 참고 자료이며, Meowney는 이를 금융 대시보드의 정보 밀도와 사용성에 맞게 재해석한다.

## 1. Product direction

- 목표는 장난스러운 자산 관리 도구가 아니라 차분하고 신뢰할 수 있는 개인 금융 제품이다.
- 한 화면에는 명확한 정보 우선순위를 만들고, 모든 카드를 같은 무게로 나열하지 않는다.
- 장식보다 데이터, 행동, 상태가 먼저 읽혀야 한다.
- 고양이 콘셉트는 제품 이름과 제한된 브랜드 표현에만 남기고 UI 문장이나 상태 표시에 반복하지 않는다.

## 2. Dashboard hierarchy

대시보드의 기본 순서는 다음과 같다.

1. 페이지 제목과 설명
2. 시장 현황
3. 리밸런싱 알림
4. 총 자산, 투자 원금, 손익, 수익률과 연간 자산 증감
5. 포트폴리오 배분과 자산 추이
6. 목표 진행률

시장 맥락에서 포트폴리오 상태로 시선이 이동하도록 이 순서를 유지한다. 새로운 기능은 중요도와 사용 빈도를 검토한 뒤 배치하며 단순히 상단에 추가하지 않는다.

## 3. Color

- Primary action: Electric Blue `#3E6AE1`
- Primary text: Carbon Dark `#171A20`
- Body text: Graphite `#393C41`
- Secondary text: Pewter `#5C5E62`
- Canvas: `#F7F7F7`
- Surface: `#FFFFFF`
- Alternate surface: `#F4F4F4`
- Divider: `#EEEEEE`

파란색은 주요 행동과 선택 상태에 사용한다. 수익·손실·경고처럼 의미 구분이 필요한 데이터에는 제한적으로 의미 색상을 허용하되 장식에는 사용하지 않는다.

## 4. Typography

- Display: `Universal Sans Display`, 시스템 sans-serif 폴백
- UI and body: `Universal Sans Text`, 시스템 sans-serif 폴백
- 본문과 UI는 기본 14px, 행간 1.43을 사용한다.
- 제목과 UI는 500, 본문은 400을 기본으로 한다.
- 700 굵기와 임의의 자간 조정은 데이터 강조가 꼭 필요한 경우에만 사용한다.
- 페이지 제목은 최대 40px을 넘기지 않는다.

## 5. Surfaces and spacing

- 페이지 배경과 카드 표면을 분리하고, 카드에는 `#EEEEEE` 수준의 얇은 테두리를 허용한다.
- 카드 그림자, 글로우, 장식용 그라데이션은 사용하지 않는다.
- 카드와 버튼의 기본 반경은 4px이며 큰 이미지 컨테이너는 최대 8~12px까지 허용한다.
- 기본 간격 단위는 8px이다.
- 카드 헤더가 있으면 콘텐츠 상단 패딩을 생략할 수 있다. 헤더 없이 `CardContent`만 사용하는 카드는 반드시 명시적인 상단 패딩을 가져야 한다.
- hover 상태에서 요소를 확대하거나 위로 이동하지 않는다. 색상과 테두리 변화만 사용한다.

## 6. Buttons and controls

- Primary: 파란 배경, 흰색 텍스트, 최소 높이 40px
- Secondary: 흰색 또는 옅은 회색 배경, Graphite 텍스트
- 한 영역의 주요 행동은 하나로 제한하고 대안 행동은 outline 또는 text action으로 낮춘다.
- 모든 상태 전환은 약 330ms를 기본으로 한다.
- 입력 필드는 테두리 색상과 얇은 focus ring으로 포커스를 표현한다.
- 탭은 채워진 segmented control보다 밑줄형 내비게이션을 우선한다.

## 7. Icons, illustrations, and emoji

- 제품 UI 아이콘은 Lucide의 1.5~1.75px 선형 스타일로 통일한다.
- 한 아이콘은 하나의 명확한 행동 또는 의미만 표현해야 한다.
- 장식성 `Cat`, `Sparkles`, `Heart`, 이모지 아이콘을 상태·빈 화면·버튼에 사용하지 않는다.
- 성공, 오류, 경고는 아이콘과 문장, 필요한 경우 의미 색상으로 표현한다.
- 빈 화면은 관련 선형 아이콘, 짧은 설명, 하나의 다음 행동으로 구성한다.
- 교육 콘텐츠에는 개념을 실제로 설명하는 차트, SVG 또는 제작된 일러스트레이션만 사용한다.
- 생성 이미지는 텍스트를 포함하지 않고 UI 캡션과 접근성 대체 텍스트를 함께 제공한다.
- 브랜드 마크는 헤더와 About 영역 등 제한된 위치에서만 사용한다.

## 8. Voice and copy

- 문장은 짧고 직접적으로 쓴다.
- `냥~`, 고양이 말투, 감정 이모지는 제품 메시지와 오류 문구에서 사용하지 않는다.
- 오류는 무엇이 실패했는지, 사용자가 무엇을 할 수 있는지 설명한다.
- 빈 상태는 상태 설명 다음에 가능한 행동을 제시한다.
- 금융 정보는 과장하거나 확정적으로 표현하지 않는다.

## 9. Responsive behavior

- 768px 미만에서는 단일 열과 모바일 내비게이션을 사용한다.
- 데이터 테이블은 중요한 열을 유지하고 보조 정보는 접거나 상세 화면으로 이동한다.
- CTA는 좁은 화면에서 전체 너비로 확장할 수 있다.
- 차트와 교육 이미지는 잘리지 않아야 하며, 핵심 레이블은 작은 화면에서도 읽을 수 있어야 한다.
- 최소 터치 목표는 40px, 주요 모바일 행동은 44px 이상을 권장한다.

## 10. Review checklist

- 첫 3초 안에 화면의 핵심 정보와 주요 행동을 알 수 있는가?
- 정보 순서가 사용자 질문의 순서와 일치하는가?
- 같은 계층의 요소가 같은 타이포그래피와 간격을 사용하는가?
- 이모지나 아이콘을 제거해도 의미가 유지되는가? 그렇다면 장식일 가능성이 높다.
- 색상 없이도 레이블과 구조만으로 상태를 이해할 수 있는가?
- 빈 상태와 오류 상태에 다음 행동이 있는가?
- 라이트·다크 모드와 모바일에서 정보가 손실되지 않는가?

